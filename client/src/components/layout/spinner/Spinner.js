import React, { Fragment } from "react";
import "./Spinner.css";

const Spinner = () => {
  return (
    <Fragment>
      <h1 id="spinner">Loading...</h1>
    </Fragment>
  );
};

export default Spinner;
